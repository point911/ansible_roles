---

dependencies:
  - role: sansible.users_and_groups
    version: v1.0
    users_and_groups:
      groups:
        - name: kafka-manager
      users:
        - name: kafka-manager
          group: kafka-manager
    tags:
      - build
