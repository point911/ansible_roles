---

kibana:
  host: localhost
  install_dir: "/usr/share/kibana4"
  port: 5601
  tarball: "kibana-4.6.1-linux-x64"

  elasticsearch:
    host: localhost
    port: 9200

  plugins:
    - plugin_name: elasticsearch/marvel/latest
      plugin_dir:  marvel
    - plugin_name: elasticsearch/timelion/latest
      plugin_dir:  timelion

